/* ============================================================
   МОДУЛЬ: ДЕЙСТВИЯ ПОСЛЕ «ДА»
   ============================================================ */
const YesActionModule = {
  init() {
    bindField('#yesShowMsg', 'yesShowMsg');
    bindField('#yesMusic', 'yesMusic');
    bindField('#yesConfetti', 'yesConfetti');
    bindField('#yesFireworks', 'yesFireworks');
    bindField('#yesHearts', 'yesHearts');
    bindField('#yesPhoto', 'yesPhoto');
    bindField('#yesVideoUrl', 'yesVideoUrl');
    bindField('#yesRedirect', 'yesRedirect');
    bindField('#yesTimer', 'yesTimer');
  }
};
